name: 'Init pipeline for update backend on test server'

on:
  push:
    branches:
      - 'dev/devops'

jobs:
  init:
    runs-on: 
      labels: test
    steps:
    
      - name: Run test-github-actions-back.yml
        uses: ./.github/workflows/test/back@main